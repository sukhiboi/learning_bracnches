const multiply = require('./src/multiply');
const subtract = require('./src/subtract');
const add = require('./src/add');
const percent = require('./src/percent');

console.log(multiply(2, 3), subtract(5, 2));
console.log(add(3, 4));
console.log(percent(10, 90));
